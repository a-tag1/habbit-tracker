# Habit Tracker ユーザーマニュアル（簡易版）

## 概要

Habit Tracker は、日々の習慣を記録・管理するアプリです。毎日・週次・月次の習慣タスクを登録し、達成状況を手軽に確認できます。データはすべてお使いの端末内に保存されます。

---

## 画面構成

アプリ下部のナビゲーションバーから 4 つの画面に移動できます。

| アイコン | 画面名 | 用途 |
|---|---|---|
| カレンダー | **デイリー** | 今日の習慣を確認・記録する |
| リスト | **タスク管理** | 習慣タスクを追加・編集・削除する |
| グラフ | **統計** | 達成率の推移を確認する |
| 歯車 | **設定** | データのバックアップ・復元を行う |

---

## 1. デイリー画面

今日のタスク一覧と達成進捗を確認する画面です。

### 日付の移動

- 画面上部の **「＜」「＞」ボタン** をタップして前後の日に移動します。
- スマートフォンでは **左右スワイプ** でも移動できます。
- **未来の日付には移動できません。**

### 進捗バー

画面上部に「完了済み数 / 全タスク数」の進捗バーが表示されます。

### タスクの状態を記録する

各タスクには 3 種類のステータスがあります。

| 状態 | 表示 | 操作方法 |
|---|---|---|
| **未実施**（pending） | グレー | 初期状態 |
| **完了**（completed） | 緑 | タスク行をタップ |
| **スキップ**（skipped） | 文字に取り消し線（アンバー） | タスク右端の **✕ボタン** をタップ |

> タップするたびに「未実施 → 完了 → 未実施」と切り替わります。✕ボタンは「スキップ ↔ 未実施」をトグルします。

### 週次・月次タスクの進捗カウンター

週次・月次タスクはタスク行の右端に `現在の達成数 / 目標回数` が表示されます。

---

## 2. タスク管理画面

習慣タスクを管理する画面です。

### タスクを追加する

1. 右下の **「＋」ボタン** をタップします。
2. 以下の項目を入力します。

| 項目 | 説明 |
|---|---|
| **タスク名** | 習慣の名称（例：ランニング、読書） |
| **繰り返し** | `毎日` / `週 X 回` / `月 X 回` から選択 |
| **目標回数** | 週次・月次の場合は **＋/－ボタン** または直接入力で回数を設定（週: 1〜7、月: 1〜31） |

3. **「保存」** をタップして完了します。

### タスクを編集する

タスク一覧の **鉛筆アイコン** をタップするとモーダルが開き、内容を変更できます。

### タスクを削除する

タスク一覧の **ゴミ箱アイコン** をタップします。確認ダイアログが表示されるので、**「削除」** を選択してください。

> 削除すると関連する履歴データもすべて失われます。ご注意ください。

### 並び替え

各タスク左端の **ハンドルアイコン（≡）** をドラッグすると、表示順を変更できます。

---

## 3. 統計画面

タスクごとの達成率をグラフで確認できる画面です。

### 表示期間の切り替え

画面上部のボタンで期間を選択します。

| ボタン | 表示内容 |
|---|---|
| **1か月** | 選択した月の達成率 |
| **3か月** | 直近 3 か月の月別達成率 |
| **6か月** | 直近 6 か月の月別達成率 |

### 基準月の変更

**「＜」「＞」ボタン** で基準月を前後に移動できます（未来月には移動できません）。

### 達成率の見方

各タスクに `完了数 / 目標数（達成率%）` が表示されます。

| 繰り返し種別 | 目標数の計算方法 |
|---|---|
| 毎日 | 当月の経過日数（過去月は月全体） |
| 週次 | 4 週間 × 目標回数 |
| 月次 | 設定した月の目標回数 |

---

## 4. 設定画面

データのバックアップと復元を行う画面です。

### データをエクスポートする（バックアップ）

1. **「エクスポート」** ボタンをタップします。
2. `habit-tracker-backup-YYYY-MM-DD.json` という名前のファイルがダウンロードされます。
3. 大切に保管してください。

### データをインポートする（復元）

1. **「インポート」** ボタンをタップします。
2. 保存済みの JSON ファイルを選択します。
3. 確認後にデータが上書き復元されます。

> **注意：** インポートすると現在のデータはすべて上書きされます。事前にエクスポートしてバックアップを取ることをお勧めします。

### データ概要

画面内に「登録タスク数」と「履歴件数」が表示されます。

---

## 5. ホーム画面に追加する（PWA）

このアプリはスマートフォンのホーム画面に追加してアプリのように使用できます。

**iOS（Safari）の場合**
1. Safari で本アプリを開きます。
2. 下部の **共有アイコン（□↑）** をタップします。
3. **「ホーム画面に追加」** を選択します。

**Android（Chrome）の場合**
1. Chrome で本アプリを開きます。
2. 右上の **「⋮」メニュー** をタップします。
3. **「ホーム画面に追加」** を選択します。

---

## データの保存について

すべてのデータはお使いの端末の **ローカルストレージ** に自動保存されます。操作のたびに即座に保存されるため、手動での保存は不要です。

> ブラウザのデータをクリアすると記録が消えてしまいます。定期的にエクスポートしてバックアップを取ることをお勧めします。
